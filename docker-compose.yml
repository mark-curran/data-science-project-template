version: '3'
services:
  prod:
    build:
      context: '.'
      args:
        REQIREMENTS_FILE: './requirements.txt'
        STARTING_DIR: '/app'
    volumes:
      - ./src:/src
    image: scrambled-words
